using Piet.Grid;

namespace Piet.Interpreter;

public sealed class CodelChooser
{
    private readonly CodelGrid _codelGrid;

    public CodelChooser(
        CodelGrid codelGrid)
    {
        _codelGrid = codelGrid;
    }

    // public Codel GetNextCodel(Codel codel)
    // {
    //     
    // }
}